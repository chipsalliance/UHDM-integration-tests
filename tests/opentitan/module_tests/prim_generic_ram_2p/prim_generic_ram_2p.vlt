`verilator_config

// That is the nature of a dual-port memory: both write ports can access the same storage simultaneously.
lint_off -rule MULTIDRIVEN -file "*" -match "Signal has multiple driving blocks with different clocking: '*.mem'*"
